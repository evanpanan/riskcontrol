feat: auth v2.1 - 登录商用化 + BD RBAC 隔离 + 退出修复 + 通知打通

1. Login页商用化（src/app/login/page.tsx）
- 移除 6 个演示账号 & 自定义账号一键登录卡片 / 快速切换区
- 仅邮箱 + 密码表单：邮箱 regex 校验 + 密码至少 6 位；字段级错误提示
- ADMIN admin@riskcontrol.io 必须严格匹配默认密码 Admin@Risk2026
- 访客白名单：仅 @institution.com / @riskcontrol.io / @fund.cn 才可登录为 OPS 访客
- 记住此设备 Checkbox（新建 src/components/ui/checkbox.tsx）、忘记密码入口、上次登录时间戳
- 商用视觉：网纹 + 渐变光斑 + 遮罩；顶栏 SOC 2 + TLS 1.3 双安全徽章

2. BD 经理角色权限分离（行级数据隔离）
- AppSidebar：/alerts /margin-calls /market /settings 仅机构角色可见（ADMIN / RISK_MANAGER / OPS）
- BD 仅保留「风控大盘」「客户管理」两项，侧边栏底部加 BD 专属工作台提示卡
- 处理补仓按钮（BatchCardV2 + /margin-calls 页）RoleGate 收紧 RISK_MANAGER | ADMIN
- 申请退出 / 标记结算 / 撤销操作（ClientTable 操作列）仅机构角色渲染
- 首页风控大盘 batches 过 filterBatchesForUser，KPI/批次/客户数只统计 BD 名下
- /clients 客户管理：BD 隐藏「批量新增客户」「导出客户表」；客户批次 Tabs 仅名下批次
- /bd/[bdName] 加访问守卫，BD 登录访问他人页面自动 replace 回自己详情

3. 退出登录链路端到端修复
- AuthProvider SESSION_UPDATED & storage 监听器空 session 不再 auto fallback 默认用户
  （避免登出 1ms 内被自动回滚为默认风控总监）
- TopBar 头像 Button 双击直接 logoutToLogin 逃生门
- logoutToLogin 用 location.replace(/login?next=...) + href 兜底硬跳转
- 三套 Radix Portal（dialog / dropdown-menu / select）移除显式 container={document.body}
  恢复默认挂载，修复 data-state 永久 closed（菜单/下拉打不开）

4. 通知真实打通（Email + WhatsApp 三层路由）
- 新建 src/app/api/notify/send/route.ts 服务端 handler
- lib/notifier RuntimeNotificationConfig + getRuntimeNotificationConfig + sendNotification
  路由：
  ① 最高优先级 = Settings 页 localStorage risk_control_settings 配置的
    emailWebhook / whatsappWebhook
  ② 次优先级 = 真实供应商密钥（RESEND_API_KEY / TWILIO_* / META_WA_*）
  ③ 兜底降级 = 全局环境变量 EMAIL_WEBHOOK_URL / WHATSAPP_WEBHOOK_URL
- Settings 页新增「发送测试邮件/WhatsApp」按钮 + 状态机
  （idle -> sending -> success/error + toast + 详细错误面板）

5. 权限工具 & 新组件
- lib/auth/index.ts 新增：
  * INSTITUTION_ROLES + hasInstitutionView + isBDManager
  * bdOwnershipMatcher（兼容 displayName = 李晓明 (Evan Li) 双写法）
  * filterBatchesForUser + filterClientsForUser（机构全量 / BD 按归属过滤）
- src/components/ui/checkbox.tsx 原生实现（aria-checked + sr-only hidden input）

6. 反向约束 & 校验通过
- FlashNumber L80 fallbackTimerRef 永远不修改（grep 6 处引用正常）
- 全项目 grep autoFocus 0 匹配（搜索框 / 登录 input 不抢焦点）
- TypeScript strict tsc --noEmit 0 error

外部资源坐标（无法从代码恢复）
- Mock Session LS key: rbac_mock_session_v1
- Settings LS key: risk_control_settings
- 新建账号 LS key: risk_control_users_v1
- Branding Logo LS key: risk_control_logo_v1
- Client Status LS key: risk_control_client_status_v1
- Notification 环境变量: 见 .env.example RESEND_API_KEY / TWILIO_* / META_WA_*
- 管理员初始账户密码: admin@riskcontrol.io / Admin@Risk2026
- Dev server: next dev -p 3002
